var ErrorDetails = function(id, show, message) {

    this.id = id,
    this.hashId = '#' + id,
    this.show = show,
    this.message = message
}

ErrorDetails.prototype = function(){

    return {};
}();